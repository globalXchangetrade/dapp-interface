import React from 'react';
import {
  Container,
  InnerContainer
} from './styles';

export const Option:React.FC = () => {
  return (
    <Container>
      <InnerContainer>
        Option
      </InnerContainer>
    </Container>
  )
};
